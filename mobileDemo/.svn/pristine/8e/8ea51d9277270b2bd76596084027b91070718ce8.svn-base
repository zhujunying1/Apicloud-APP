.header#header
    .left.back(tapmode,onclick= closeTo == 1 ? "my_close()" : "api.closeWin()")
        i.icon-back
    case headCon
        when 0
            .center=cont
        default
            .left-next=cont
