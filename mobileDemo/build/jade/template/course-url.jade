extends ../layout/layout
block body
    -var closeTo = 1
    -var cont = 'ICFE财务英语标准课ACCA前导'
    include ../modules/header
block scripts
    script(type='text/javascript').
        apiready = function () {
            $('.left-txt').html(api.pageParam.title);
            api.openFrame({
                name: 'out_url',
                url: api.pageParam.url,
                bounces: true,
                showProgress: true,
                delay: 200
            });
        };